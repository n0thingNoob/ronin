// scalar_add_math.cpp — generated by mydsl codegen
// Source spec: src/codegen/specs/scalar_add.py
// Compute: c = a + b
// DO NOT EDIT. Regenerate via prj/codegen/scalar_add/gen.sh

void kernel(
        pipe<T> pa,
        pipe<T> pb,
        pipe<T> pc,
        uint32 num_blocks,
        uint32 block_tiles) {
    for (uint32 block = 0; block < num_blocks; block++) {
        pc.reserve_back();
        pa.wait_front();
        pb.wait_front();
        math<T> acc;
        for (uint32 i = 0; i < block_tiles; i++) {
            acc.add(pa, pb, i, i, i);
        }
        for (uint32 i = 0; i < block_tiles; i++) {
            acc.pack(i, pc);
        }
        pa.pop_front();
        pb.pop_front();
        pc.push_back();
    }
}
