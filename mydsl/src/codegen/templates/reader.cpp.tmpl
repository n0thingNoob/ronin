// ${name}_reader.cpp — generated by mydsl codegen
// Source spec: src/codegen/specs/${name}.py
// DO NOT EDIT. Regenerate via prj/codegen/${name}/gen.sh

void kernel(
${global_args},
${pipe_args},
${pos_args},
        uint32 num_blocks,
        uint32 block_tiles) {
    uint32 block_items = block_tiles * 1024;
    for (uint32 i = 0; i < num_blocks; i++) {
${reserve_block}
${read_block}
        read_barrier();
${push_block}
${pos_inc_block}
    }
}
