// ${name}_math.cpp — generated by mydsl codegen
// Source spec: src/codegen/specs/${name}.py
// Compute: ${compute_repr}
// DO NOT EDIT. Regenerate via prj/codegen/${name}/gen.sh

void kernel(
${pipe_args},
        uint32 num_blocks,
        uint32 block_tiles) {
    for (uint32 block = 0; block < num_blocks; block++) {
${reserve_out_block}
${wait_in_block}
        math<T> acc;
        for (uint32 i = 0; i < block_tiles; i++) {
            ${compute_call}
        }
        for (uint32 i = 0; i < block_tiles; i++) {
${pack_block}
        }
${pop_in_block}
${push_out_block}
    }
}
